source 'https://rubygems.org'

gem 'rails', '3.2.13'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

group :development do
  gem 'sqlite3', '1.3.5'
end

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '3.2.4'
  gem 'coffee-rails', '3.2.2'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '1.2.5'
end

# gem 'jquery-rails', '2.0.0'  # specified by book, but not available
gem 'jquery-rails', '2.3.0'    # last 2.x version
# gem 'jquery-rails', '3.1.4'  # version installed with Rails 3.1.3, but 3.x doesn't include
#                                jQuery UI, per https://github/rails/jquery-rails, where it says
#                                to use jquery-ui-rails gem instead. Not sure what that's all
#                                about, but think I'd prefer a 2.x version now, if possible.

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
